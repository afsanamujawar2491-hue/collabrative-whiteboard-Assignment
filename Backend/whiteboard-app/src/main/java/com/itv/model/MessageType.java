package com.itv.model;

public enum MessageType {
    OBJECT_ADDED,
    OBJECT_MODIFIED,
    OBJECT_REMOVED,
    CLEAR,
    UNDO,
    REDO,
    CURSOR_MOVE,
    CHAT,
    STATE_REQUEST,
    STATE_SYNC
}
